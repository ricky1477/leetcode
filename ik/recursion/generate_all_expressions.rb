# Complete the generate_all_expressions function below.
def generate_all_expressions(s, target)
  helper(s, index, list, result, target)
end

def helper(string, index, list, result, target)
  # Base case
  if (index == string.length)
      result.add(slate)
  # Recursive case
  else
      
  end
  
  
end